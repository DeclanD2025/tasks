# Dependencies for the web layer only (app.web) — deliberately excludes
# PySide6/pyqtgraph so the container image stays small and headless.
# Keep versions in sync with pyproject.toml.
SQLAlchemy>=2.0
pandas>=2.2
numpy>=1.26
platformdirs>=4.2
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
APScheduler>=3.10
pdfplumber>=0.11
pypdf>=6.0
fastapi>=0.111
uvicorn>=0.30
jinja2>=3.1
python-multipart>=0.0.9
