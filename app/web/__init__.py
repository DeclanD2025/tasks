"""ORION web access layer.

Serves the same local database as the desktop app through a mobile-first
webapp (FastAPI + Jinja). Runs on this machine only; open it from a phone on
the same network. No hosted backend, no external APIs, no LLM.
"""
